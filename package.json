{
  "name": "swarm-constants",
  "version": "0.0.1",
  "description": "UI Constants for Meetup's Swarm Design System",
  "scripts": {
	 "lint": "yarn run eslint ./properties/**/*.json",
    "build": "yarn run lint && node scripts/build.js"
  },
  "author": "adamd@meetup.com",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^4.1.1",
    "eslint-plugin-json": "^1.2.0",
    "style-dictionary": "^2.0.0-beta",
    "tinycolor2": "^1.4.1"
  }
}
